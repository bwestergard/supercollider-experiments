{
var note = 34;
var freq = note.midicps;
var tone = SinOsc.ar(freq, phase: LocalIn.ar);
LocalOut.ar(VarSaw.ar(freq*2, 0, LFTri.ar(3).range(0.001,0.5)) * LFTri.ar(3).range(0,1));
LocalOut.ar(DelayC.ar(tone * Line.ar(0,0.9,2), 3, 1/(3*freq)));
Out.ar(0,tone.dup * 0.3);
}.play;
