z = {
	var note = 37+0;
	var dur = 0.3;
	BMoog.ar(
		LFTri.ar(XLine.ar(note.midicps/4,note.midicps,dur*0.065)),
		Line.kr(10000, note.midicps * 0.5,3*dur/2), // cutoff freq.
		Line.kr(0.01, 0.7, dur/5), // q
		0, // mode - lowpass
		2).dup * 2 * Line.ar(1,0,dur) // mul
}.play
